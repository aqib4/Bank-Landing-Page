import styles from "../Style";

const Button = () => (
    <div>
          <button className={`font-poppins text-black text-[18px] bg-blue-gradient rounded py-2 px-4 font-medium`}>Get Started</button>
    </div>
    
)

export default Button;
